server:
  port: 8085
  
spring:
  application:
    name: CONFIG-SERVER  
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/vinayakurade/micro-service-config"
          clone-on-start: true

